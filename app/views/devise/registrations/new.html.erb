<div class="page-background padding-navbar row">
<!-- <script src="http://code.jquery.com/jquery-1.11.1.min.js"></script>
<script src="http://cdn.jsdelivr.net/jquery.validation/1.15.0/jquery.validate.min.js"></script>
<script src="http://cdn.jsdelivr.net/jquery.validation/1.15.0/additional-methods.min.js"></script>

<script>
    $(document).ready(function () {
        $("#new_account").validate({
            debug: true,
            rules: {
                "account[name]": {required: true},
                "account[password]": {required: true, minlength: 6},
                "account[password_confirmation]": {equalTo: "#account_password"},
                "account[email]": {required: true, email: true},
                "account[phone_number]": {required: true,  phoneUS: true},
                "account[gender]": {required: true}
            }
        });
    });
</script> -->
<div class="col-md-8 col-md-offset-2">
  <div class="registration signup-container">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <legend>Register</legend>

        <div class="form-group col-lg-12 field">
            <%= f.label :name %><br />
            <%= f.text_field :name, autofocus: true, :class => "form-control", :placeholder=>"Name" %>
        </div>

        <div class="form-group col-lg-6 field">
            <%= f.label :password %>
            <% if @minimum_password_length %>
                <em>(<%= @minimum_password_length %> characters minimum)</em>
            <% end %><br />
            <%= f.password_field :password, autocomplete: "off", :class => "form-control",:placeholder=>"Password"%>
        </div>

        <div class="form-group col-lg-6 field">
            <%= f.label :password_confirmation %><br />
            <%= f.password_field :password_confirmation, autocomplete: "off", :class => "form-control", :placeholder=>"Confirm Password" %>
        </div>

        <div class="form-group col-lg-6 field">
            <%= f.label :email %><br />
            <%= f.email_field :email, :class => "form-control", :placeholder=>"Email"%>
        </div>

        <div class="form-group col-lg-6 field">
            <%= f.label :phone_number %><br />
            <%= f.text_field :phone_number, :class => "form-control", :placeholder=>"phone_number" %>
        </div>

        <div class="form-group col-lg-12 field">
          <%= f.label :image, "Choose and upload profile image"%>
          <%= f.file_field :image %>
          <%= f.hidden_field :image_cache %>
        </div>

        <div class="form-group col-lg-12 field">
            <%= f.label :gender %><br />
            <label class="radio-inline"><%= f.radio_button :gender, "m" %>Male</label>
            <label class="radio-inline"><%= f.radio_button :gender, "f" %>Female</label>
        </div>

        <p>By clicking the Continue button below, you agree to the Terms of Agreement</p>

        <div class="actions">
            <%= f.submit "Accept and Sign up", :class=>"btn btn-success btn-large form-control" %>
        </div>

      <% end %>
    </div>
  </div>
</div>
<!-- <%= render "devise/shared/links" %> -->
